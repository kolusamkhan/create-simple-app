export const echo = (message: any) => {
  console.log(`Received ${message} by ${Date.toString()}`);
  return message;
};


export const echoV1 = (message: any) => {
  console.log(`Received ${message} by ${Date.toString()}`);
  return message;
};